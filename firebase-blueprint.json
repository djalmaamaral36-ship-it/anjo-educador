{
  "entities": {
    "students": {
      "title": "Student Pax Data",
      "description": "Real-time synced student data including presence, timer status, activities, and logs",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "nome": { "type": "string" },
        "presenca": { "type": "object" },
        "timelineEvents": { "type": "array" }
      },
      "required": ["id", "nome"]
    }
  },
  "securityRules": {
    "students": {
      "read": "true",
      "write": "true"
    }
  }
}
